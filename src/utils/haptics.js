/**
 * Haptic Feedback Utility
 * Provides haptic feedback for user interactions (no-op for web)
 */

/**
 * Trigger haptic feedback for button press
 */
export const triggerButtonHaptic = () => {
  // No-op for web version
  // Can be extended with native haptics if needed
};
