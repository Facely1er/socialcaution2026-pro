# Netlify Configuration for Basic Version (socialcaution.com)
# Use this config file for the Basic/Free tier site

[build]
  command = "npm run build:basic"
  publish = "dist-simple"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"
  VITE_APP_VERSION = "basic"

# Production deployment
[context.production]
  command = "npm run build:basic"
  [context.production.environment]
    VITE_REACT_APP_ENVIRONMENT = "production"
    VITE_APP_MODE = "basic"

# SPA fallback - all routes go to index.html
[[redirects]]
  from = "/*"
  to = "/index-simple.html"
  status = 200
  force = false

# Security headers
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    X-XSS-Protection = "1; mode=block"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=()"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline' https://www.google-analytics.com https://www.googletagmanager.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' https://fonts.gstatic.com; connect-src 'self' https://www.google-analytics.com; base-uri 'self'; form-action 'self'"

# Cache static assets (1 year)
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Cache service worker (no cache)
[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Don't cache HTML files
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

# Function configuration
[functions]
  node_bundler = "esbuild"
  included_files = ["netlify/functions/**"]

